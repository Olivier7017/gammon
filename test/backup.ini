[pytest]
addopts = --strict-markers --mypy
markers =
    slow: marks tests as slow (use pytest slow)
    fast: marks tests as fast (use pytest fast)
filterwarnings =
    ignore:.*is deprecated:DeprecationWarning
    ignore:.*torch:FutureWarning
    ignore:.*zip file:UserWarning
